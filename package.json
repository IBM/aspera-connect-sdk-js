{
  "version": "3.11.1",
  "description": "Top level project for the IBM Aspera Connect SDK",
  "private": true,
  "scripts": {
    "build": "run-s build:browser build:carbon build:bundle",
    "build:browser": "npm --prefix packages/browser run build",
    "build:bundle": "./scripts/bundle-sdk.sh",
    "build:carbon": "npm --prefix packages/carbon-installer run build",
    "build:skip-zip": "run-s build:browser build:carbon build:bundle",
    "build:zip": "./scripts/zip-sdk.sh",
    "bump": "node ./scripts/bump-versions.js",
    "clean": "rimraf *.zip *ConnectSDK* dist packages/browser/docs packages/browser/esm packages/browser/build packages/browser/dist",
    "preinstall": "node ./scripts/preinstall.js",
    "release": "standard-version --no-verify --path packages/ --skip.bump=true",
    "test:browser": "npm --prefix packages/browser run test:integration -- --browsers ChromeCustom"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "glob": "^7.1.6",
    "husky": "^2.3.0",
    "npm-run-all": "^4.1.5",
    "replace-in-file": "^4.2.0",
    "rimraf": "^2.6.3",
    "standard-version": "^9.1.0"
  },
  "dependencies": {},
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}

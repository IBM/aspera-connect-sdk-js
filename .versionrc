{
  "message": "chore(release): %s",
  "scripts": {
    "postbump": "cd packages/browser && npm version $(node -pe 'require(\"../../package.json\").version') --allow-same-version && cd ../.. && cd packages/carbon-installer && npm version $(node -pe 'require(\"../../package.json\").version') --allow-same-version && cd ../.."
  },
  "commitUrlFormat": "https://gitlab.aspera.us/dwosk/ibm-aspera-connect-sdk/commit/{{hash}}",
  "types": [
    { "type": "feat", "section": "Features" },
    { "type": "fix", "section": "Bug Fixes" },
    { "type": "chore", "hidden": true },
    { "type": "docs", "hidden": true },
    { "type": "style", "hidden": true },
    { "type": "refactor", "hidden": true },
    { "type": "perf", "hidden": true },
    { "type": "test", "hidden": true },
    { "type": "build", "hidden": true }
  ]
}